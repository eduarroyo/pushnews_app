<Alloy>
	<Window id="index">
		<View id="mainView" layout="vertical">
			<Widget src="com.appbando.cabecera" id="viewCabecera" />
			<View id="mapview" module="ti.map" onClick="pulso" userLocation="true" animate="true">
	            <!-- Starting with Alloy 1.4.0, use the Annotation tag to define an annotation -->
	            <!-- Prior to Alloy 1.4.0, create annotations in the controller -->
	            <Annotation id='cliente' myid='1' />
	        </View>
		</View>
	</Window>
</Alloy>